name: "CodeQL Analysis"

on:
  push:
  pull_request:
  
jobs:
  codeql:
    name: CodeQL Analysis
    uses: nasa/cFS/.github/workflows/codeql-build.yml@main
    with: 
      make-prep: 'make prep'
      make: 'make ci_lab'